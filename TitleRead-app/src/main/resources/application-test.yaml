spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  cache:
    type: redis

jwt:
  secret: test-secret-key-for-integration-tests-only
  expiration: 3600000
  refresh-expiration: 86400000